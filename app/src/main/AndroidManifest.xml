<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ratingapp.ddey.com.dam_project">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.general.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.general.MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.general.RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.general.AboutActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.general.EditProfileActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.general.BaseDrawerActivity" />
        <activity android:name=".activities.student.JoinPrivateQuizActivity" />
        <activity android:name=".activities.student.JoinPublicQuizActivity" />
        <activity android:name=".activities.teacher.NewQuizActivity" />
        <activity android:name=".activities.teacher.NewQuestionActivity" />
        <activity android:name=".activities.teacher.InactiveQuizzesActivity" />
        <activity android:name=".activities.teacher.ActiveQuizzesActivity" />
        <activity android:name=".activities.teacher.ActivateQuizActivity" />
        <activity android:name=".activities.teacher.QuizAccessTypeActivity" />
        <activity android:name=".activities.teacher.StudentsResultsActivity" />
        <activity android:name=".activities.student.QuizInformationActivity" />
        <activity android:name=".activities.student.QuizActivity" />
        <activity android:name=".activities.student.QuizResultActivity" />
        <activity android:name=".activities.student.QuizHistoryActivity" />
        <activity android:name=".activities.general.SettingsActivity" />
        <activity android:name=".activities.student.PublicQuizzesActivity" />
    </application>

</manifest>